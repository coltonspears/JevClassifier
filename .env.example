# Server-only key. Never prefix this with VITE_ or place it in frontend code.
OPENROUTER_API_KEY=
JEV_MODEL=typesafe/jev-1.13
PORT=3001
# Local-only by default. Set to 0.0.0.0 only when intentionally sharing the demo.
HOST=127.0.0.1
